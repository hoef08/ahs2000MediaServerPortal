services:
  backend:
    build: ./backend
    restart: unless-stopped
    env_file: ./backend/.env
    environment:
      - NODE_ENV=development
      - PORT=3000
      - TZ=Europe/Zurich
    ports:
      - "3000:3000"
    volumes:
      - token-cache:/app/data
    networks:
      - media-net

  frontend:
    build: ../frontend
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    depends_on:
      - backend
    networks:
      - media-net

networks:
  media-net:

volumes:
  token-cache:
